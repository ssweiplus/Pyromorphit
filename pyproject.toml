[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pyromorphit"
version = "0.2.0"
description = "Agent-first human-in-the-loop red-team harness"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["pyromorphit*"]
